🚀 CHUNK 2 — REPLIT AGENT PATCH

Agent — apply EXACTLY as written:
Modify only the Daily Stock V2 form file.


---

🔧 PATCH START

(Copy this into the Replit agent)

--- MODIFY FILE: client/src/pages/forms/DailyStockV2Form.tsx
+++ ADD PURCHASED STOCK INPUTS TO FORM 2

@@
  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-6">

+     {/* ===========================================================
+        PURCHASED STOCK SECTION — NEW
+     ============================================================ */}
+     <div className="rounded-lg border border-gray-200 bg-white shadow-sm">
+       <div className="bg-yellow-300 px-4 py-3 rounded-t-lg">
+         <h2 className="text-lg font-bold text-gray-900">Purchased Stock</h2>
+       </div>
+       <div className="p-4 space-y-6">

+         {/* ROLLS PURCHASED */}
+         <div>
+           <label className="block text-sm font-semibold text-gray-700 mb-1">
+             Rolls Purchased (units)
+           </label>
+           <input
+             type="number"
+             {...register("rollsPurchased", { valueAsNumber: true })}
+             className="w-full rounded border border-gray-300 px-3 py-2"
+             placeholder="e.g. 140"
+             min="0"
+           />
+         </div>

+         {/* MEAT PURCHASED */}
+         <div>
+           <label className="block text-sm font-semibold text-gray-700 mb-1">
+             Meat Purchased
+           </label>
+
+           <div className="flex gap-3">
+             <input
+               type="number"
+               step="0.01"
+               {...register("meatPurchasedValue", { valueAsNumber: true })}
+               className="flex-1 rounded border border-gray-300 px-3 py-2"
+               placeholder="e.g. 3"
+               min="0"
+             />
+
+             <select
+               {...register("meatPurchasedUnit")}
+               className="w-24 rounded border border-gray-300 px-2 py-2"
+             >
+               <option value="kg">kg</option>
+               <option value="g">g</option>
+             </select>
+           </div>
+
+           <p className="text-xs text-gray-500 mt-1">
+             Enter weight and choose unit. System converts everything to grams.
+           </p>
+         </div>

+         {/* DRINKS PURCHASED */}
+         {drinkSkus?.length > 0 && (
+           <div>
+             <label className="block text-sm font-semibold text-gray-700 mb-2">
+               Drinks Purchased (per SKU)
+             </label>
+
+             <div className="space-y-3">
+               {drinkSkus.map((sku: string) => (
+                 <div key={sku} className="flex items-center gap-4">
+                   <div className="w-40 text-sm font-medium">{sku}</div>
+                   <input
+                     type="number"
+                     {...register(`drinksPurchased.${sku}`, { valueAsNumber: true })}
+                     className="flex-1 rounded border border-gray-300 px-3 py-2"
+                     placeholder="0"
+                     min="0"
+                   />
+                 </div>
+               ))}
+             </div>
+           </div>
+         )}

+       </div>
+     </div>



@@
     {/* EXISTING SECTION — END OF STATION STOCK COUNTS */}
     <div className="rounded-lg border border-gray-200 bg-white shadow-sm">
       <div className="bg-yellow-300 px-4 py-3 rounded-t-lg">
         <h2 className="text-lg font-bold text-gray-900">End Stock Counts</h2>
       </div>
       <div className="p-4 space-y-6">

         {/* Rolls End Count (existing) */}
         <div>
           <label className="block text-sm font-semibold text-gray-700 mb-1">
             Rolls End Count (units)
           </label>
           <input
             type="number"
             {...register("rollsEnd", { valueAsNumber: true })}
             className="w-full rounded border border-gray-300 px-3 py-2"
             min="0"
           />
         </div>

         {/* Meat End Count — KG (existing) */}
         <div>
           <label className="block text-sm font-semibold text-gray-700 mb-1">
             Meat End Count (kg)
           </label>
           <input
             type="number"
             step="0.01"
             {...register("meatEndKg", { valueAsNumber: true })}
             className="w-full rounded border border-gray-300 px-3 py-2"
             min="0"
           />
         </div>

         {/* DRINKS END COUNT (existing layout, unaffected) */}

🔧 PATCH END


---