import { useEffect, useState } from "react";
export default function PosKitchen(){const [orders,setOrders]=useState<any[]>([]);const [error,setError]=useState("");const load=()=>fetch('/api/pos/kitchen/orders',{credentials:'include'}).then(r=>r.json()).then(x=>{if(!x.ok)throw Error(x.error);setOrders(x.data||[])}).catch(e=>setError(e.message));useEffect(()=>{load();const t=setInterval(load,10000);return()=>clearInterval(t)},[]);const status=(id:string,s:string)=>fetch(`/api/pos/orders/${id}/status`,{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:s})}).then(load);return <main><div className="mb-5 flex items-center justify-between"><div><p className="font-bold text-yellow-600">SBB POS</p><h1 className="text-3xl font-black">Kitchen tickets</h1></div><button className="rounded bg-black px-4 py-2 text-white" onClick={load}>Refresh</button></div>{error&&<p className="mb-4 rounded bg-red-50 p-3 text-red-700">{error}</p>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{orders.map(o=><article key={o.id} className="rounded-xl border bg-white p-4 shadow-sm"><div className="flex justify-between"><strong>{o.ticket_number}</strong><span className="rounded bg-yellow-100 px-2 py-1 text-xs font-bold">{o.order_mode}</span></div><ul className="my-4 space-y-2">{o.items?.map((i:any)=><li key={i.id}><strong>{i.quantity} × {i.item_name_en}{i.is_set_component?' (SET)':''}</strong>{i.modifiers?.map((m:any,index:number)=><p key={index} className="text-sm text-green-700">+ {m.name_en}</p>)}{i.notes&&<p className="mt-1 rounded bg-red-50 px-2 py-1 text-sm font-bold text-red-800">REQUEST: {i.notes}</p>}</li>)}</ul><button className="w-full rounded bg-green-600 p-3 font-bold text-white" onClick={()=>status(o.id,'ready')}>Mark ready & call ticket</button></article>)}</div>{!orders.length&&!error&&<p className="rounded border bg-white p-8 text-center text-neutral-500">No active POS tickets.</p>}</main>}
