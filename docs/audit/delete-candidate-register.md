# Delete Candidate Register

Conservative register separating runtime code from archive/assets noise.

## Runtime code candidates (requires owner sign-off)
No runtime deletions proposed in this report-only correction PR.

## Asset/archive duplication candidates
- `archive/**` (legacy snapshots, not mounted at runtime).
- `attached_assets/**` pasted transcripts and ad-hoc artifacts.
- `tmp/**` generated exports and one-off logs (if present).
