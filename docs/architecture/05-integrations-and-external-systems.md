# Integrations and External Systems

| Integration | Evidence |
|---|---|
| OpenAI | See env var and import evidence in machine-readable JSON + route/service files |
| Google APIs/Drive/Sheets | See env var and import evidence in machine-readable JSON + route/service files |
| Loyverse | See env var and import evidence in machine-readable JSON + route/service files |
| SCB payments | See env var and import evidence in machine-readable JSON + route/service files |
| LINE notify | See env var and import evidence in machine-readable JSON + route/service files |
| GitHub API | See env var and import evidence in machine-readable JSON + route/service files |
| Object storage | See env var and import evidence in machine-readable JSON + route/service files |
| Nodemailer/email | See env var and import evidence in machine-readable JSON + route/service files |
| Replit plugins | See env var and import evidence in machine-readable JSON + route/service files |
